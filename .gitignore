# Local Claude Code state
/.claude/settings.local.json
/.claude/worktrees/

# Node, only ever present because of markdownlint-cli2
node_modules/
package-lock.json

# Secrets — never tracked
.env
.env.local

# Windows artefact from an accidental `> nul`
nul

# Editor and OS noise
# NOTE: .vscode/ is deliberately NOT ignored — it is shared project configuration.
.idea/
*.swp
Thumbs.db
desktop.ini

# Rendered output, should it ever be generated locally
/_site/
/book/

# Normalise line endings to LF in the repository and in the working tree, to
# match the kernel repo and the Linux containers everything is built in.
* text=auto eol=lf

# Binaries must never be normalised — archived prior art and artwork.
*.png  binary
*.jpg  binary
*.zip  binary
*.pdf  binary
